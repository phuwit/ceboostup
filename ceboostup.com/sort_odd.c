// บรรทัดแรกจะบอกจำนวนข้อมูลตัวเลขทั้งหมดที่มีอยู่ n ตัว
// บรรทัดที่สองจะให้ข้อมูลตัวเลขทั้งหมด เว้นระหว่างตัวเลขด้วยช่องว่าง
// พี่อยากให้เอาตัวเลขคี่ในข้อมูลที่ให้มาเรียงใหม่จากน้อยไปมาก แล้วส่งข้อมูลตัวเลขทั้งหมดที่เรียงแล้วกลับมา

#include <stdio.h>

int main () {
    
}